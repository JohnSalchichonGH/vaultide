/**
 * Completed-month completeness and freshness (blueprint 12.6, v2.1.15 30.18).
 * Pure: every date, record and resolution is passed in, and nothing is stored.
 */
export * from './types';
export * from './month';
